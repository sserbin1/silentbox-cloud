{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npm install -g pnpm && pnpm install && pnpm --filter @silentbox/booking build",
  "outputDirectory": ".next",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://api.silentbox.io"
  }
}
