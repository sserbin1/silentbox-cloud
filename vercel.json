{
  "version": 2,
  "builds": [
    {
      "src": "apps/admin/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "cd ../.. && npm install -g pnpm && pnpm install",
        "buildCommand": "cd ../.. && pnpm --filter @silentbox/shared build && cd apps/admin && pnpm build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/apps/admin/$1"
    }
  ]
}
