{
  "version": 2,
  "buildCommand": "npm install -g pnpm && pnpm install && pnpm --filter @silentbox/shared build && pnpm --filter @silentbox/admin build",
  "outputDirectory": "apps/admin/.next",
  "builds": [
    {
      "src": "apps/admin/package.json",
      "use": "@vercel/next"
    }
  ]
}
